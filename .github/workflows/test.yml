name: Test

on:
  # push 不限分支：dev 之类的试验分支推上去也验一遍，不用先合进 main 才知道有没有坏。
  push:
  pull_request:
    branches: [main, template]

jobs:
  go:
    name: Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod # 用 go.mod 里声明的版本，和本地保持一致

      - name: Vet
        run: go vet ./...

      - name: Test with coverage
        run: bash ./gotest.sh

      - name: Show coverage gaps # 打印总覆盖率和未达 100% 的函数，方便定位
        if: always()
        run: |
          echo "===== TOTAL ====="
          go tool cover -func=coverage.txt | tail -n 1
          echo "===== functions below 100% ====="
          go tool cover -func=coverage.txt | grep -v '100.0%' || true

      - name: Upload coverage profile
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-profile
          path: coverage.txt

  python:
    name: Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Read Python version
        id: python-version
        run: |
          major="$(sed -n 's/^PYTHON_MIN_MAJOR=//p' scripts/init.sh)"
          minor="$(sed -n 's/^PYTHON_MIN_MINOR=//p' scripts/init.sh)"
          echo "version=$major.$minor" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ steps.python-version.outputs.version }}

      - name: Install deps
        run: pip install -r requirements-dev.txt

      - name: Test
        run: PYTEST=pytest bash ./pytest.sh

  java:
    name: Java
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Read Java version
        id: java-version
        run: |
          version="$(sed -n 's/^JAVA_RELEASE="${JAVA_RELEASE:-\([0-9]*\)}"/\1/p' javatest.sh)"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: ${{ steps.java-version.outputs.version }}

      - name: Test
        run: bash ./javatest.sh
