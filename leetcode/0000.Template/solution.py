"""题解骨架。把函数名和签名改成本题要求的，例如 Two Sum 是 twoSum(nums, target)。

同一题的多种解法写在本文件里的多个函数，入口函数保持 LeetCode 给的签名名
（LeetCode 的 Python 模板用 camelCase，这里跟随它，文件内保持一致），
其余解法加后缀说明算法：twoSum / twoSumBruteForce / twoSumTwoPointers。

辅助类直接写在本文件里就行。conftest.py 的 fixture 只加载 solution.py，
拆成多个模块反而要自己处理 import。
"""


def solve(nums):
    return []
