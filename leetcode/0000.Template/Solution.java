// ===== 本地接线区 =====
// LeetCode 的 Java 编辑器预置了 java.util.*，本地要显式 import，写在这里。
// 用到 TreeNode / ListNode 不用写任何东西，javatest.sh 编译时会带上 structures/java。

// ===== 以下是题解本体，必须和提交到 LeetCode 的代码一字不差 =====

/**
 * 题解骨架。把方法名和签名改成本题要求的，例如 Two Sum 是 int[] twoSum(int[] nums, int target)。
 *
 * <p>类声明是 class Solution 而不是 public class Solution——LeetCode 给的模板就是不带
 * public 的（带 public 的是 TreeNode）。文件名仍叫 Solution.java，非 public 类不要求
 * 和文件名匹配。
 *
 * <p>同一题的多种解法写在本类里的多个方法，入口方法保持 LeetCode 给的签名名，
 * 其余解法加后缀说明算法：twoSum / twoSumBruteForce / twoSumTwoPointers。
 *
 * <p>辅助类如果声明成 public，Java 强制它必须单独一个文件，按结构命名（SegmentTree.java）；
 * 不写 public 就能和题解放在同一个文件里。
 */
class Solution {

    public int[] solve(int[] nums) {
        return new int[] {};
    }
}
